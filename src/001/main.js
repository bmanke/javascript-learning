console.log('  Running  '.trim); // Trims whitespace
console.log(' Together '.length); // Gets the length of the string
console.log(' Forever '.toUpperCase()); // Converts to upper case
console.log(Math.random() * 100); // Generates a random
console.log(Math.floor(Math.random() * 100)); // Rounds to the nearest whole number
console.log(Math.ceil(43.8)); // Calculates the smallest integer greater than or equal to
console.log(Number.isInteger(2018)); // Checks if the value is an integer